export type Category = {
  id: string
  name: string
  slug: string
  count: number // Added tool count
  svg: string // Added image URL
  // Add any other properties your category objects might have
}
